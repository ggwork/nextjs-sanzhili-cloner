# 2026-08-09

## 创建 /contact 和 /message 独立页面 (方案B)

发现项目遗漏了目标站点两个独立页面：
- 客户留言 Message.aspx?ClassID=62
- 联系我们 Contact.aspx?ClassID=40

之前的实现把两个页面合并到 `/contact` 路径，且该路径没有实际页面路由（404）。

### 变更清单
- **新增** `src/app/[locale]/contact/page.tsx` — 联系我们页面(Header activeIndex=6)，复用 ContactForm
- **新增** `src/app/[locale]/message/page.tsx` — 客户留言页面(Header activeIndex=4)，使用新 MessageForm
- **新增** `src/components/message-form.tsx` — 留言表单组件(POST /api/contact)
- **修改** `src/data/site.ts` — nav.guestbook href /contact→/message；新增 CONTACT_BANNER/MESSAGE_BANNER
- **修改** `src/app/sitemap.ts` — 添加 /message 路由
- **修改** `messages/zh.json` / `messages/en.json` — 新增 banners/pageTitles/message 翻译
- **修改** `src/components/cta-section.tsx` — href #contact→/contact

### 技术说明
- TypeScript 类型检查通过 (npx tsc --noEmit)
- next build 因 safe-delete 问题使用 distDir=.nextbuild 临时绕过

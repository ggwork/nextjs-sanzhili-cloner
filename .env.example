# Contact form recipient email (server-side)
CONTACT_EMAIL=
# Public contact email (exposed to client-side, e.g. mailto links)
NEXT_PUBLIC_CONTACT_EMAIL=
# Contact phone number (exposed to client-side, e.g. tel links)
NEXT_PUBLIC_CONTACT_PHONE=

# SMTP settings (e.g. 163 mail: smtp.163.com / 465)
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
